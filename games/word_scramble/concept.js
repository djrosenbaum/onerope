// var countries = ['usa', 'canada', 'england'];

var numbers = [];
var scrambled_numbers;

var country = 'australia';
// var random_country = [];
// var total_letters = country.length;

for ( var i=0; i < country.length; i++ ) {

	numbers.push(i);

	//var random_number = Math.ceil(Math.random() * total_letters);

}


for ( var i=0; i < country.length; i++ ) {

	var random_number = Math.ceil(Math.random() * numbers.length);

	scrambled_numbers[i] = numbers[random_number];

	numbers

}